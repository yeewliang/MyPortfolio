version: '3.8'

services:
  portfolio:
    build: .
    container_name: my-portfolio
    ports:
      - "8081:80"
    restart: unless-stopped
    environment:
      - TZ=UTC
    labels:
      - "com.docker.compose.project=my-portfolio"